# ? Server Configuration
server.port=9090
spring.application.name=GreetingApplication

# ? MySQL Database Configuration (Updated to use 'greetingdb')
spring.datasource.url=jdbc:mysql://localhost:3306/greetingdb?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=@shuklaJi723
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ? Hibernate Configuration
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect
spring.jpa.hibernate.ddl-auto=update

# ? Show SQL Queries in Console (optional)
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# ? Disable H2 Console (since you're using MySQL now)
spring.h2.console.enabled=false

# ? Email Configuration (Keep the same)
spring.mail.properties.mail.smtp.ssl.protocols=TLSv1.2
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=divyanshshuklaofficial23@gmail.com
spring.mail.password=rsazsqoroqxnocid
spring.mail.properties.mail.smtp.auths=true
spring.mail.properties.mail.smtp.starttls.enable=true
